query VaultPrice($vault_address: ID!) {
  beta_clm(id: $vault_address) {
    priceOfToken0InToken1
    priceRangeMin1
    priceRangeMax1
  }
  clm(id: $vault_address) {
    priceOfToken0InToken1
    priceRangeMin1
    priceRangeMax1
  }
}
